
name: 'Market_Basket'
version: '1.0.0'
config-version: 2

profile: 'snowflake-db'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  
clean-targets:        
  - "target"
  - "dbt_packages"

models:
  Market_Basket:
    staging:
      materialized: view
      snowflake_warehouse: compute_wh
    marts:
      materialized: table
      snowflake_warehouse: compute_wh